# Auto detect text files and normalize line endings to LF
* text=auto eol=lf

# Windows-specific scripts (CRLF allowed if needed)
*.bat text eol=crlf
*.cmd text eol=crlf

# Mark binary files to prevent Git from messing with them
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.exe binary
